@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find dependencies
find_dependency(Threads)

# Import targets
include("${CMAKE_CURRENT_LIST_DIR}/HybridAStarTargets.cmake")

# Set version
set(HybridAStar_VERSION @PROJECT_VERSION@)

# Check if the package is found
if(NOT HybridAStar_FOUND)
    set(HybridAStar_FOUND TRUE)
endif()