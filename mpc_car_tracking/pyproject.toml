[project]
name = "mpc-car-tracking"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kuisongzheng", email = "2436809937@qq.com" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy>=1.24.0",
    "scipy>=1.10.0",
    "matplotlib>=3.7.0",
    "cvxpy>=1.4.0",
    "control>=0.9.0",
    "pytest>=8.4.1",
]

[project.scripts]
mpc-car-tracking = "mpc_car_tracking:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
